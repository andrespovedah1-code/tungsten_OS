name: One-off suggested goals repair
'on':
  push:
    branches:
      - goal-suggestions-fix
    paths:
      - .github/workflows/goal-suggestions-fix.yml
permissions:
  contents: write
jobs:
  validate-runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "workflow parser and runner are healthy"
